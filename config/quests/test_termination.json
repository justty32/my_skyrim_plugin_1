{
  "id": "test_termination",
  "title": "Termination — complete_quest stops the action chain and all triggers (SPEC §3.1.3)",
  "version": 1,
  "vars": { "n": 0 },
  "on_start": [
    { "show_message": "starting; about to complete the quest" },
    { "complete_quest": true },
    { "add_var": { "var": "n", "value": 99 } },
    { "show_message": "BUG if shown: action after complete_quest ran" }
  ],
  "triggers": [
    { "on": "ping",
      "do": [
        { "add_var": { "var": "n", "value": 1 } },
        { "show_message": "BUG if shown: terminated quest responded to a trigger" }
      ] },
    { "on": "timer", "key": "late",
      "do": [ { "show_message": "BUG if shown: terminated quest fired a timer" } ] }
  ]
}
